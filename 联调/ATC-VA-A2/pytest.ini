[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -m "not network and not e2e and not longrun"
markers =
    unit: unit tests
    integration: integration tests
    network: real LiveATC network tests
    e2e: end-to-end tests
    longrun: long stability tests
